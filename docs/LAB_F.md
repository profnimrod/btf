# Lab F — walkthrough

Full method: Appendix F. GPU stages expose `--plan` so you can inspect the
exact configuration on a laptop before running on a CUDA host, e.g.:

```bash
python train/sft.py --plan          # Lab C
python quant/awq.py --plan          # Lab E
```

See the README table for the module ↔ lab mapping.
